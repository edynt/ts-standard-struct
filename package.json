{
  "name": "typescript-node-project",
  "version": "1.0.0",
  "description": "TypeScript Node.js project with Docker",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.x",
    "express": "^4.x",
    "kafkajs": "^2.x",
    "redis": "^4.x",
    "dotenv": "^16.x",
    "cors": "^2.x",
    "helmet": "^7.x"
  },
  "devDependencies": {
    "@types/express": "^4.x",
    "@types/node": "^20.x",
    "@types/cors": "^2.x",
    "prisma": "^5.x",
    "ts-node-dev": "^2.x",
    "typescript": "^5.x"
  }
}